"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toInclude = toInclude;

var _predicate = _interopRequireDefault(require("./predicate"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const passMessage = (utils, actual, expected) => () => utils.matcherHint('.not.toInclude') + '\n\n' + 'Expected string to not include:\n' + `  ${utils.printExpected(expected)}\n` + 'Received:\n' + `  ${utils.printReceived(actual)}`;

const failMessage = (utils, actual, expected) => () => utils.matcherHint('.toInclude') + '\n\n' + 'Expected string to include:\n' + `  ${utils.printExpected(expected)}\n` + 'Received:\n' + `  ${utils.printReceived(actual)}`;

function toInclude(actual, expected) {
  const pass = (0, _predicate.default)(actual, expected);

  if (pass) {
    return {
      pass: true,
      message: passMessage(this.utils, actual, expected)
    };
  }

  return {
    pass: false,
    message: failMessage(this.utils, actual, expected)
  };
}