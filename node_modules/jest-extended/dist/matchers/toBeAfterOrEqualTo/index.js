"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toBeAfterOrEqualTo = toBeAfterOrEqualTo;

var _predicate = _interopRequireDefault(require("./predicate"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const passMessage = (utils, received, before) => () => utils.matcherHint('.not.toBeAfterOrEqualTo', 'received', '') + '\n\n' + `Expected date to be after or equal to ${utils.printReceived(before)} but received:\n` + `  ${utils.printReceived(received)}`;

const failMessage = (utils, received, before) => () => utils.matcherHint('.toBeAfterOrEqualTo', 'received', '') + '\n\n' + `Expected date to be after or equal to ${utils.printReceived(before)} but received:\n` + `  ${utils.printReceived(received)}`;

function toBeAfterOrEqualTo(date, after) {
  const pass = (0, _predicate.default)(date, after);

  if (pass) {
    return {
      pass: true,
      message: passMessage(this.utils, date, after)
    };
  }

  return {
    pass: false,
    message: failMessage(this.utils, date, after)
  };
}